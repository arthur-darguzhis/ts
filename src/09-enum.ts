enum ShapeKind {
    Circle,
    Square,
}

const sample = ShapeKind.Circle


enum StatusCode {
    ERROR = 500,
    NOT_FOUND = 404,
    NOT_AUTH = 403,
}


enum Grades {
    Junior = 'junior',
    Middle = 'middle',
    Senior = 'senior',
}
